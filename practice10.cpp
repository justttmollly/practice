#include <iostream>
#include <cmath>
using namespace std;
int main() {
double x = 1.7,d;
int c = 3,e;
 d = pow((x + 1), 2) + 3 * (x + 1);
 e = pow((c + 1), 2) + 3 * (c + 1);
cout << "a)" << d << endl << "b)" << e;
}

